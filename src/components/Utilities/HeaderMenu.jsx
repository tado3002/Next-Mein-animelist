import React from "react";

const HeaderMenu = ({title}) => {
  return <div className="flex justify-center pt-8 pb-4 text-lg font-bold uppercase transform">{title}</div>;
};

export default HeaderMenu;
